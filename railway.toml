[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
nixpacksPlan = { providers = ["node"], variables = { NODE_VERSION = "20" } }

[deploy]
startCommand = "node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
